# Backend

FastAPI backend implementation will be added after the first trained model artifacts are available.

Planned endpoints:

- `POST /signup`
- `POST /login`
- `POST /predict`
- `POST /train`
- `GET /similar-rooms`
- `GET /history`

